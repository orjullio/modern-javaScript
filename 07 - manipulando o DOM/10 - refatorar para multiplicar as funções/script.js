function criarBotao(classes) {
     const button = document.createElement("button");
     botao.className = classe;
     return botao;    
}

function criarIcon(classes) {
    const icon = document.createElement("i");
    icon.className = classes;
    return icon;
}
